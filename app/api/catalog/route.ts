import {NextResponse} from'next/server';import{products}from'../../../lib/catalog';export async function GET(request:Request){const url=new URL(request.url);const slug=url.searchParams.get('slug');if(slug)return NextResponse.json({product:products.find(p=>p.slug===slug)??null});return NextResponse.json({products,categories:['clubs','world-cup','retro','fan-gear']})}
