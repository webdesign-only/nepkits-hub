'use client';
import { useState } from 'react';
import { useRouter,useSearchParams } from 'next/navigation';
import Link from 'next/link';
import { supabase } from '@/lib/supabase-browser';
export default function LoginPage(){const router=useRouter();const params=useSearchParams();const [signup,setSignup]=useState(false);const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [busy,setBusy]=useState(false);const [message,setMessage]=useState('');async function submit(){setBusy(true);const result=signup?await supabase.auth.signUp({email,password,options:{data:{full_name:name}}}):await supabase.auth.signInWithPassword({email,password});setBusy(false);if(result.error){setMessage(result.error.message);return}router.push(params.get('next')||'/account')}return <main className='auth-page'><div className='auth-visual'><img src='https://images.unsplash.com/photo-1526232761682-d26e03ac148e?auto=format&fit=crop&w=1500&q=88' alt='Football culture'/><div><span>NEPKITS HUB</span><h1>ENTER<br/><em>THE GAME.</em></h1></div></div><div className='auth-panel'><img src='/nepkits-logo.webp' alt='NEPKITS HUB' className='auth-logo'/><span className='auth-eyebrow'>{signup?'CREATE ACCOUNT':'MEMBER LOGIN'}</span><h2>{signup?'JOIN THE HUB.':'WELCOME BACK.'}</h2><p>{signup?'Build your profile, save your favourites and keep your orders in one place.':'Your orders, wishlist and saved details are waiting.'}</p>{signup&&<input className='auth-input' value={name} onChange={e=>setName(e.target.value)} placeholder='Full name'/>}<input className='auth-input' value={email} onChange={e=>setEmail(e.target.value)} placeholder='Email'/><input className='auth-input' type='password' value={password} onChange={e=>setPassword(e.target.value)} placeholder='Password'/>{message&&<div className='auth-error'>{message}</div>}<button className='auth-submit' onClick={submit} disabled={busy}>{busy?'PLEASE WAIT…':signup?'CREATE ACCOUNT':'SIGN IN'}</button><button className='auth-switch' onClick={()=>setSignup(!signup)}>{signup?'Already a member? Sign in':'New here? Create an account'}</button><Link href='/shop' className='auth-back'>← BACK TO SHOP</Link></div></main>}